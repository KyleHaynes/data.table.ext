% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R, R/zz-dbdt.R
\name{duckdt_csv}
\alias{duckdt_csv}
\alias{dbdt_csv}
\title{Open a CSV file as a duckdt view}
\usage{
duckdt_csv(path, conn = NULL, name = NULL, ...)

dbdt_csv(path, conn = NULL, name = NULL, ...)
}
\arguments{
\item{path}{Path to one or more CSV files.}

\item{conn}{A \code{DBI} connection to DuckDB; a new in-memory one is created
if \code{NULL}.}

\item{name}{View name to use inside DuckDB. Defaults to the file's base
name.}

\item{...}{Currently unused.}
}
\value{
A \code{"duckdt"} object (a view, i.e. not writable via \verb{:=}).
}
\description{
Creates a lazy DuckDB view over the file using \code{read_csv_auto} — the file
is not read into R; querying is pushed down and executed out-of-core in
DuckDB, \code{fread}-style ergonomics included. This is inherently DuckDB-only
(there's no MS SQL Server equivalent of \code{read_csv_auto}); the resulting
\code{"duckdt"} handle can still be pointed at from a DuckDB connection only.
}
