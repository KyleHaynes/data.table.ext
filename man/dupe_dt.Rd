% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling-and-switches.R
\name{dupe_dt}
\alias{dupe_dt}
\title{Surface duplicate rows}
\usage{
dupe_dt(dt, by = NULL, color = .sample_dt_color_default())
}
\arguments{
\item{dt}{A data.table.}

\item{by}{Character vector of column names defining a duplicate. If \code{NULL}
(default), all columns are used.}

\item{color}{Logical scalar. If \code{TRUE} (default), tag output for colored
grouped display.}
}
\value{
A data.table of duplicate rows, ordered by cluster, with an added
\code{.dupe_group} integer column identifying each cluster. Returns a
zero-row data.table if no duplicates are found.
}
\description{
Returns every row that participates in a duplicate cluster (two or more
rows sharing the same values across \code{by}), ordered by cluster and tagged
with grouped-print attributes so \code{enable_dt_print_thousands()} draws
separators and colors between clusters, the same way \code{cdt()} does.
}
\examples{
DT <- data.table::data.table(id = c(1, 1, 2, 3), name = c("a", "a", "b", "c"))
dupe_dt(DT)
dupe_dt(DT, by = "id")
}
