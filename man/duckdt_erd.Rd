% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erd.R
\name{duckdt_erd}
\alias{duckdt_erd}
\title{Visualize a database's tables and foreign-key relationships}
\usage{
duckdt_erd(conn, include_row_counts = FALSE, open = TRUE)
}
\arguments{
\item{conn}{A \code{DBI} connection, or a \code{"duckdt"} object (its \verb{$conn} is
used).}

\item{include_row_counts}{Include a \verb{SELECT count(*)} per table in the
diagram. Default \code{FALSE}, since this can be slow on a large or remote
database; a failure on any single table is skipped rather than failing
the whole call.}

\item{open}{Open the generated HTML file with \code{\link[utils:browseURL]{utils::browseURL()}}.
Default \code{TRUE}.}
}
\value{
Invisibly, the path to the generated HTML file. The Mermaid
diagram source is attached as the \code{"mermaid"} attribute, so it can be
dropped directly into an Rmd/Quarto \verb{```mermaid} code chunk.
}
\description{
Introspects \code{conn}'s tables, columns, primary keys, and foreign keys via
the portable \code{information_schema} views (works against both DuckDB and MS
SQL Server connections), renders them as a Mermaid \code{erDiagram}, and opens
the result as a self-contained HTML page in the system browser. Useful for
quickly seeing what tables are available in a database and how they
connect, without writing any SQL yourself.
}
\details{
Foreign keys are only detected when the database actually declares them as
constraints — this does not guess relationships from column-naming
conventions.
}
