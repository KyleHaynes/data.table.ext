% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erd.R
\name{duckdt_erd}
\alias{duckdt_erd}
\title{Explore a database's tables and relationships in your browser}
\usage{
duckdt_erd(
  x,
  tables = NULL,
  include_row_counts = FALSE,
  open = TRUE,
  infer_references = FALSE,
  select = NULL,
  view = c("all", "keys_only", "title_only"),
  title = NULL,
  file = NULL
)
}
\arguments{
\item{x}{A \code{DBI} connection, a \code{"duckdt"} object, or a
\code{"duckdt_data_model"} (from \code{\link[=duckdt_data_model]{duckdt_data_model()}}, so you can filter or
annotate it first).}

\item{tables}{Optionally, a character vector of tables to model. Others
are left out entirely.}

\item{include_row_counts}{Run a \verb{SELECT count(*)} per table and show it
next to each table. Default \code{FALSE}, since this can be slow on a large
or remote database; a failure on any single table shows as no count
rather than failing the whole call.}

\item{open}{Open the generated page with \code{\link[utils:browseURL]{utils::browseURL()}}. Default
\code{TRUE}.}

\item{infer_references}{Guess undeclared foreign keys from column naming
conventions, via \code{\link[=duckdt_dm_infer_references]{duckdt_dm_infer_references()}}. Default \code{FALSE}.}

\item{select}{Tables to start with ticked. Defaults to all of them when
there are 12 or fewer, otherwise none.}

\item{view}{Initial level of detail: \code{"all"} columns, \code{"keys_only"} or
\code{"title_only"}.}

\item{title}{Page title. Defaults to a description of the connection.}

\item{file}{Where to write the page. Defaults to a temporary file.}
}
\value{
Invisibly, the path to the generated HTML file, with the Mermaid
diagram source attached as its \code{"mermaid"} attribute (so it can be
dropped straight into an Rmd/Quarto \verb{```mermaid} chunk) and the
data model as its \code{"data_model"} attribute.
}
\description{
Builds a data model (\code{\link[=duckdt_data_model]{duckdt_data_model()}}) and writes it to a
self-contained HTML page: an ER diagram of the tables you tick, a
searchable list of every table and column, and -- as you tick columns --
the \code{duckdt}/SQL code that selects exactly those, ready to copy. Useful
for finding your way around a database you didn't build, without writing
any SQL first. Works against both DuckDB and MS SQL Server connections,
and needs no packages beyond duckdt itself.
}
\details{
Only foreign keys the database actually declares as constraints are drawn.
DuckDB files built by loading CSV/Parquet usually declare none, in which
case pass \code{infer_references = TRUE} to guess them from column names (see
\code{\link[=duckdt_dm_infer_references]{duckdt_dm_infer_references()}} for exactly what that guesses), or state
them yourself with \code{\link[=duckdt_dm_add_references]{duckdt_dm_add_references()}} and pass the resulting
model straight to this function.

The page renders its diagram with \href{https://mermaid.js.org/}{Mermaid}
loaded from a CDN. With no internet connection everything still works --
the table/column browser, the generated code -- except the drawing itself,
which is replaced by its source.
}
\examples{
con <- duckdt_connect(quiet = TRUE)
DBI::dbExecute(con, "CREATE TABLE customers (id INTEGER PRIMARY KEY, name VARCHAR)")
DBI::dbExecute(con, "CREATE TABLE orders (
  id INTEGER PRIMARY KEY, customer_id INTEGER REFERENCES customers(id), total DOUBLE)")

path <- duckdt_erd(con, open = FALSE)
cat(attr(path, "mermaid"))
DBI::dbDisconnect(con, shutdown = TRUE)
}
\seealso{
\code{\link[=duckdt_explorer]{duckdt_explorer()}} for the Shiny version, which also previews
and runs the query; \code{\link[=duckdt_dm_mermaid]{duckdt_dm_mermaid()}} and \code{\link[=duckdt_dm_dot]{duckdt_dm_dot()}} for the
diagram sources on their own.
}
