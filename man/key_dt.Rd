% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-eda.R
\name{key_dt}
\alias{key_dt}
\title{Find candidate key columns}
\usage{
key_dt(dt, cols = NULL, max_size = 3L)
}
\arguments{
\item{dt}{A data.table.}

\item{cols}{Character vector of candidate columns to consider. If \code{NULL}
(default), all columns are considered.}

\item{max_size}{Maximum number of columns to combine when searching.
Default \code{3L}. Search cost grows combinatorially with both \code{max_size}
and the number of \code{cols}, so raise this cautiously on wide tables.}
}
\value{
A data.table with one row per candidate key found, columns \code{cols}
(comma-separated column names) and \code{n_cols}, ordered by \code{n_cols}
ascending. Returns a zero-row data.table if no candidate is found.
}
\description{
Searches column combinations (up to \code{max_size} columns) for one that
uniquely identifies every row of \code{dt}, useful for spotting a natural key
before a join. Reports only minimal candidates: a combination is skipped
if a smaller already-found candidate is a subset of it.
}
