% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example.R, R/zz-dbdt.R
\name{duckdt_example}
\alias{duckdt_example}
\alias{dbdt_example}
\title{An example database to try things on}
\usage{
duckdt_example(quiet = FALSE)

dbdt_example(quiet = FALSE)
}
\arguments{
\item{quiet}{Skip the message describing what was created.}
}
\value{
A \code{DBI} connection to a new in-memory DuckDB database. Close it
with \code{\link[=dbdt_disconnect]{dbdt_disconnect()}}.
}
\description{
Creates a small in-memory DuckDB database -- four tables of a toy shop
schema, with real primary and foreign keys declared, and a few rows in
each. Somewhere to point \code{\link[=dbdt_erd]{dbdt_erd()}}, \code{\link[=dbdt_explorer]{dbdt_explorer()}} or
\code{\link[=dbdt_data_model]{dbdt_data_model()}} when you want to see what they do before aiming
them at a database of your own.
}
\details{
The tables are \code{customers}, \code{items}, \code{orders} and \code{order_lines};
\code{order_lines} has a compound primary key and references both \code{orders} and
\code{items}, so the diagram has something to draw.
}
\examples{
con <- dbdt_example(quiet = TRUE)
dbdt_tables(con)
dbdt_data_model(con)

d <- dbdt(con, "orders")
d[, .N, by = customer_id]

dbdt_disconnect(con)
}
