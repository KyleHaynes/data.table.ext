% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query-builder.R, R/zz-dbdt.R
\name{duckdt_dm_query}
\alias{duckdt_dm_query}
\alias{dbdt_dm_query}
\title{Build a SELECT statement from a data model}
\usage{
duckdt_dm_query(dm, tables, columns = NULL, where = NULL, limit = NULL)

dbdt_dm_query(dm, tables, columns = NULL, where = NULL, limit = NULL)
}
\arguments{
\item{dm}{A \code{"duckdt_data_model"} from \code{\link[=dbdt_data_model]{dbdt_data_model()}}, or anything
\code{\link[=dbdt_data_model]{dbdt_data_model()}} accepts.}

\item{tables}{Character vector of tables to select from. The first is the
one everything else joins to.}

\item{columns}{Optionally, a named list mapping table name to the columns
wanted from it. Tables not named contribute all their columns; a table
mapped to \code{character(0)} contributes none.}

\item{where}{Optional SQL predicate, inserted as the \code{WHERE} clause
verbatim.}

\item{limit}{Optional row limit.}
}
\value{
A length-1 character vector of SQL, with the tables that could not
be joined in its \code{"unjoined"} attribute.
}
\description{
Given the tables (and columns) you want, this works out how to join them
from the model's references and writes the SQL -- the same query the
\code{\link[=dbdt_erd]{dbdt_erd()}} page and \code{\link[=dbdt_explorer]{dbdt_explorer()}} show you as you tick boxes,
available on its own so you can run or edit it.
}
\details{
Tables are joined (with \verb{LEFT JOIN}) along the references in the model,
starting from the first one you name. A table with no reference path to
the others can't be joined automatically: it is left out of the \code{FROM}
clause and reported in the \code{"unjoined"} attribute, rather than silently
becoming a cross join.
}
\examples{
dm <- dbdt_data_model(list(
  orders = data.frame(id = 1L, customer_id = 1L, total = 1),
  customers = data.frame(id = 1L, name = "a")
))
dm <- dbdt_dm_add_references(dm, orders$customer_id == customers$id)
cat(dbdt_dm_query(dm, c("orders", "customers"),
                    columns = list(orders = "total", customers = "name")))
}
