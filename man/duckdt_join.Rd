% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join.R, R/zz-dbdt.R
\name{duckdt_join}
\alias{duckdt_join}
\alias{merge.duckdt}
\alias{dbdt_join}
\title{Join two tables inside the database}
\usage{
duckdt_join(
  x,
  y,
  by = NULL,
  by.x = NULL,
  by.y = NULL,
  all = FALSE,
  all.x = all,
  all.y = all,
  suffixes = c(".x", ".y"),
  sort = TRUE
)

\method{merge}{duckdt}(x, y, ...)

dbdt_join(
  x,
  y,
  by = NULL,
  by.x = NULL,
  by.y = NULL,
  all = FALSE,
  all.x = all,
  all.y = all,
  suffixes = c(".x", ".y"),
  sort = TRUE
)
}
\arguments{
\item{x, y}{The two tables to join: each a \code{"duckdt"} object or a
\code{data.frame}/\code{data.table}.}

\item{by}{Column(s) to join on, present in both. Defaults to every column
common to both (a natural join, like base \code{\link[=merge]{merge()}}).}

\item{by.x, by.y}{Join on differently-named columns. Give both, of equal
length; they pair up positionally. Overrides \code{by}. As in base \code{\link[=merge]{merge()}},
the result keeps \code{by.x}'s names.}

\item{all}{Keep non-matching rows from both sides (a \code{FULL} join)? Default
\code{FALSE} -- an \code{INNER} join, keeping only matched rows.}

\item{all.x}{Keep non-matching rows from \code{x} (a \code{LEFT} join)? Defaults to
\code{all}.}

\item{all.y}{Keep non-matching rows from \code{y} (a \code{RIGHT} join)? Defaults to
\code{all}.}

\item{suffixes}{Length-2 character vector disambiguating non-join columns
that both sides have, as in base \code{\link[=merge]{merge()}}.}

\item{sort}{Sort the result by the join columns? Default \code{TRUE}, matching
base \code{\link[=merge]{merge()}}. \code{FALSE} returns the engine's join order, which is faster
and arbitrary.}

\item{...}{Passed on to \code{\link[=dbdt_join]{dbdt_join()}}.}
}
\value{
A \code{data.table}.
}
\description{
A database-side \code{\link[=merge]{merge()}}: \code{x} and \code{y} are joined in SQL and only the
result comes back to R. Either side may be a \code{"duckdt"} handle or an
ordinary \code{data.frame}/\code{data.table} -- R-side inputs are staged into a
temporary table on the connection first, so the join itself always runs in
the engine rather than in R.
}
\details{
This is \emph{not} \code{\link[=dbdt_merge]{dbdt_merge()}}. \code{dbdt_join()} reads: it computes a new
result set and returns it, leaving both inputs untouched, the way base
\code{\link[=merge]{merge()}} does. \code{dbdt_merge()} writes: it reconciles \code{y} \strong{into} \code{x}'s
table in place (update/insert/delete). Reach for this one to combine
tables, and for that one to patch a table.

\code{merge(x, y, ...)} on a \code{"duckdt"} object dispatches here, so the familiar
spelling works too.

At least one of \code{x}/\code{y} must be a \code{"duckdt"} object -- that is what
supplies the connection to run on. If both are, they must be on the same
connection. Joining two R data.frames is data.table's job, not this
function's.
}
\section{Binary columns}{

Binary columns (\code{BLOB}/\code{BIT} on DuckDB, \code{varbinary}/\code{binary}/\code{image} on
MS SQL Server) are dropped from the result, since no driver hands them
back as an R vector. This happens after \code{by} is resolved, so a natural
join still keys on exactly the columns base \code{\link[=merge]{merge()}} would -- a binary
column used as a join key is kept.
}

\examples{
d <- as.dbdt(datasets::mtcars, name = "cars")
labels <- data.frame(cyl = c(4, 6, 8), label = c("four", "six", "eight"))
head(dbdt_join(d, labels, by = "cyl"), 3)
}
\seealso{
\code{\link[=dbdt_temp]{dbdt_temp()}} to keep a filtered subset in the database so it
can be joined; \code{\link[=dbdt_merge]{dbdt_merge()}} for the write-path counterpart.
}
