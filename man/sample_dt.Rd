% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling-and-switches.R
\name{sample_dt}
\alias{sample_dt}
\title{Sample rows from a data.table with optional group expansion}
\usage{
sample_dt(
  dt,
  n = 10,
  group = NULL,
  color = .sample_dt_color_default(),
  sort_coverage = TRUE,
  color_threshold = 500L
)
}
\arguments{
\item{dt}{A data.table to sample from.}

\item{n}{Integer sample size. Row count when \code{group = NULL}; number of
groups when \code{group} is provided.}

\item{group}{Optional grouping column, either unquoted (for example \code{group = d})
or as a character scalar (for example \code{group = "d"}).}

\item{color}{Logical scalar. If \code{TRUE} (default), annotate sampled output so
print output colors distinct character/factor values by default (distinct
mode). If needed, this will also auto-enable
\code{enable_dt_print_thousands()} so coloring appears immediately.}

\item{sort_coverage}{Logical scalar. If \code{TRUE} (default), sort rows by
commonality within each selected group: rows with more frequent values
appear first. Only applies when \code{group} is supplied.}

\item{color_threshold}{Integer scalar. If any group has more rows than this,
color output is disabled for the returned table. Matches the same parameter
on \code{cdt()}.}
}
\value{
A data.table. If \code{group} is supplied, returned rows include all
members of selected groups and include print attribute
\code{".group_print_column"}.
}
\description{
Samples from \code{dt}. If \code{group} is \code{NULL}, samples \code{n} rows.
If \code{group} is provided, samples \code{n} unique group values and returns all rows
for those selected groups. The output is ordered by \code{group} and tagged for
grouped-print separators when used with \code{enable_dt_print_thousands()}.
When \code{sort_coverage = TRUE} (default), rows are sorted by value commonality
(most frequent values first) within each selected group.
}
