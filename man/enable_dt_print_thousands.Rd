% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print-mask.R
\name{enable_dt_print_thousands}
\alias{enable_dt_print_thousands}
\title{Enable data.table print masking with row-index commas and class styling}
\usage{
enable_dt_print_thousands(
  big.mark = ",",
  color = TRUE,
  class_colors = NULL,
  show_ncol = TRUE,
  color_group_values = TRUE,
  similarity_max_distance = 2L,
  similarity_max_relative = 0.3,
  group_value_mode = c("distinct", "similarity"),
  force_color = TRUE,
  group_sep_fmt = "--------- Group: \%s"
)
}
\arguments{
\item{big.mark}{Character scalar used as the thousands separator for row
indices and \code{str()} row counts.}

\item{color}{Logical scalar. If \code{TRUE} (default), color class tokens in the
class row using the \code{cli} package.}

\item{class_colors}{Named character vector or list mapping class tokens such
as \code{"<num>"} to \code{cli} color helpers (for example \code{"col_blue"}). If
\code{NULL}, defaults are used.}

\item{show_ncol}{Logical scalar. If \code{TRUE} (default), print \verb{ncol: <value>}
above the table.}

\item{color_group_values}{Logical scalar. If \code{TRUE} (default), and grouped
display is active via \code{".group_print_column"}, color character/factor
values within each group.}

\item{similarity_max_distance}{Integer scalar edit-distance threshold used
for grouping similar values when \code{color_group_values = TRUE}.}

\item{similarity_max_relative}{Numeric scalar relative edit-distance
threshold used alongside \code{similarity_max_distance}.}

\item{group_value_mode}{Character scalar: \code{"distinct"} (default) or
\code{"similarity"}. In \code{"distinct"} mode, every distinct string within each
group/column gets its own color.}

\item{force_color}{Logical scalar. If \code{TRUE}, force ANSI color output by
temporarily setting \code{options(cli.num_colors = 256)} during printing.}

\item{group_sep_fmt}{A \code{sprintf}-style format string with one \verb{\\\%s}
placeholder used to format group separator lines. Default:
\code{"--------- Group: \\\%s"}.}
}
\value{
Invisibly returns \code{TRUE}.
}
\description{
Masks \code{print.data.table} in the global environment for the current R session.
The mask delegates to the original data.table printer, then post-processes
the printed output to add thousands separators in row indices, preserve
alignment, optionally color class tokens (for example \verb{<num>}), and
optionally prepend a column-count header.
}
