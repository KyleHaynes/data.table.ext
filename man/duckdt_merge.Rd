% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge.R, R/zz-dbdt.R
\name{duckdt_merge}
\alias{duckdt_merge}
\alias{dbdt_merge}
\title{Merge a subset into a duckdt table, inside the database}
\usage{
duckdt_merge(x, y, by = NULL, update = TRUE, insert = TRUE, delete = FALSE)

dbdt_merge(x, y, by = NULL, update = TRUE, insert = TRUE, delete = FALSE)
}
\arguments{
\item{x}{A \code{"duckdt"} object backed by a \strong{materialized, writable} table
(see \code{\link[=as.dbdt]{as.dbdt()}}'s \code{copy} argument, and \code{\link[=dbdt]{dbdt()}}'s \code{writable}
argument) -- \code{dbdt_merge()} mutates \code{x} in place, so this can't be a
read-only view, nor a handle from \code{dbdt(conn, table)} that hasn't
opted into \code{writable = TRUE}.}

\item{y}{The subset to merge in: a \code{data.frame}/\code{data.table}, or another
\code{"duckdt"} object (table, view, or query result) on the \emph{same}
connection as \code{x}. Columns present in \code{y} but not in \code{x} are ignored --
use \verb{:=} first if you need to add a column.}

\item{by}{Key column(s) used to match rows between \code{x} and \code{y}. Defaults
to \emph{every} column common to both (a natural join, mirroring base
\code{\link[=merge]{merge()}}) -- since a column can't be both a match key and something
\code{update} sets from \code{y}, pass \verb{by=} explicitly whenever \code{y} carries value
columns you want updated rather than matched on.}

\item{update}{Update matching rows' shared non-key columns from \code{y}?
Default \code{TRUE}.}

\item{insert}{Insert rows present in \code{y} but not \code{x}? Default \code{TRUE}.}

\item{delete}{Delete rows present in \code{x} but not \code{y}? Default \code{FALSE};
set \code{TRUE} for a "replace this subset entirely" merge.}
}
\value{
\code{x}, invisibly.
}
\description{
Loads \code{y} into a temporary table on \code{x}'s connection, then reconciles \code{x}
against it entirely in SQL: matching rows are updated, rows only in \code{y}
are inserted, and (opt-in via \code{delete = TRUE}) rows only in \code{x} are
deleted. This is a database-side upsert/merge -- \code{x}'s existing data never
round-trips through R, and the staging table is dropped once the merge
completes.
}
\details{
On DuckDB this is emulated with \verb{UPDATE ... FROM}, an anti-join \code{INSERT},
and (if \code{delete = TRUE}) an anti-join \code{DELETE}, wrapped in a transaction.
On MS SQL Server it compiles to a single native T-SQL \code{MERGE} statement.
}
