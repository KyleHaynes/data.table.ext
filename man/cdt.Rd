% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling-and-switches.R
\name{cdt}
\alias{cdt}
\title{Display all rows grouped for printing}
\usage{
cdt(
  dt,
  group = NULL,
  color = .sample_dt_color_default(),
  sort_coverage = TRUE,
  color_threshold = 500L
)
}
\arguments{
\item{dt}{A data.table to display.}

\item{group}{Optional grouping column, either unquoted or as a character
scalar. If \code{NULL}, the full table is colored as one display unit.}

\item{color}{Logical scalar. If \code{TRUE} (default), annotate output so print
output colors distinct character/factor values by default.}

\item{sort_coverage}{Logical scalar. If \code{TRUE} (default), sort rows by
commonality within each group: rows with more frequent values appear first.}

\item{color_threshold}{Integer scalar. If any group has more rows than this,
color output is disabled for the returned table.}
}
\value{
A data.table with grouped-print attributes.
}
\description{
Returns the full table with grouped-print attributes attached so
\code{enable_dt_print_thousands()} can color values by group.
If any group exceeds \code{color_threshold}, color output is disabled for that
result.
}
