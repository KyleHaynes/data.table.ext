% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temp.R
\name{duckdt_temp}
\alias{duckdt_temp}
\title{Run a data.table-style query into a temporary table}
\usage{
duckdt_temp(x, i, j, by, name = NULL)
}
\arguments{
\item{x}{A \code{"duckdt"} object.}

\item{i, j, by}{As in \verb{[.duckdt} -- row filter, column selection/computation,
and grouping. All optional; \code{duckdt_temp(x)} copies the whole table.}

\item{name}{Name for the temporary table. Defaults to a generated name
unique within the session.}
}
\value{
A \code{"duckdt"} object over the temporary table.
}
\description{
\code{d[i, j, by]} returns a \code{data.table} -- the rows come back to R. This runs
exactly the same query, but leaves the result in the database as a
temporary table and hands back a \code{"duckdt"} handle over it. Nothing
crosses into R, so the subset stays available to anything that takes a
handle: \code{\link[=duckdt_join]{duckdt_join()}}, \code{\link[=duckdt_merge]{duckdt_merge()}}, another \code{duckdt_temp()}, or
plain \code{[} when you finally do want the rows.
}
\details{
On DuckDB the result is a genuine \code{TEMP} table: it lives in the session's
\code{temp} schema and disappears when the connection closes. On MS SQL Server
it is created with \verb{SELECT ... INTO} as an ordinary table on the current
schema (matching how \code{\link[=duckdt_merge]{duckdt_merge()}} stages there), so it outlives the
session -- call \code{\link[=duckdt_drop]{duckdt_drop()}} when you are done with it.

The handle is materialized and writable, so \verb{:=} and \code{\link[=duckdt_merge]{duckdt_merge()}} both
work against it. Writes land on the temporary copy and leave \code{x}'s table
untouched.
}
\examples{
d <- as.duckdt(datasets::mtcars)
sixes <- duckdt_temp(d, cyl == 6)
nrow(sixes)
duckdt_drop(sixes)
}
\seealso{
\code{\link[=duckdt_drop]{duckdt_drop()}} to drop it, \code{\link[=duckdt_join]{duckdt_join()}} to join it.
}
