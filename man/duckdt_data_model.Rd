% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-model.R, R/zz-dbdt.R
\name{duckdt_data_model}
\alias{duckdt_data_model}
\alias{duckdt_data_model.duckdt}
\alias{duckdt_data_model.default}
\alias{duckdt_data_model.list}
\alias{duckdt_data_model.data.frame}
\alias{dbdt_data_model}
\title{Build a data model from a database, or from data frames}
\usage{
duckdt_data_model(x, ...)

\method{duckdt_data_model}{duckdt}(x, ...)

\method{duckdt_data_model}{default}(
  x,
  tables = NULL,
  infer_references = FALSE,
  row_counts = FALSE,
  ...
)

\method{duckdt_data_model}{list}(x, ...)

\method{duckdt_data_model}{data.frame}(x, ...)

dbdt_data_model(x, ...)
}
\arguments{
\item{x}{A \code{DBI} connection, a \code{"duckdt"} object, a named list of data
frames, or a column-info \code{data.frame} (with at least \code{table} and
\code{column} columns, optionally \code{type}, \code{key}, \code{ref}, \code{ref_col}).}

\item{...}{Passed on to methods.}

\item{tables}{Optionally, a character vector restricting the model to
these tables. Matched against the bare table name or \code{"schema.name"}.}

\item{infer_references}{Also guess references from column naming
conventions, via \code{\link[=dbdt_dm_infer_references]{dbdt_dm_infer_references()}}. Default \code{FALSE}.}

\item{row_counts}{Run a \verb{SELECT count(*)} per table and record it on the
model. Default \code{FALSE}, since this can be slow on a large or remote
database; a failure on any single table records \code{NA} rather than
failing the whole call.}
}
\value{
An object of class \code{"duckdt_data_model"}: a list of three
\code{data.table}s.
\describe{
\item{\code{tables}}{One row per table: \code{table} (the identity used
everywhere else -- the bare name, or \code{"schema.name"} if that name
occurs in more than one schema), \code{schema}, \code{name}, \code{type},
\code{n_rows}, \code{segment} (cluster to draw it in) and \code{display} (colour
palette to draw it with).}
\item{\code{columns}}{One row per column: \code{table}, \code{column}, \code{type},
\code{key} (0 = not a key, otherwise its position in the primary key),
\code{ref} (referenced table, or \code{NA}), \code{ref_col} (referenced column).}
\item{\code{references}}{One row per referencing column: \code{table},
\code{column}, \code{ref}, \code{ref_col}, \code{ref_id} (rows sharing an id form one
compound reference) and \code{ref_col_num}.}
}
}
\description{
A "data model" is a portable description of a set of tables, their
columns, and the references between them -- the same idea (and largely the
same shape) as the object used by the
\href{https://github.com/bergant/datamodelr}{datamodelr} package, which this is
a port of. It is the thing \code{\link[=dbdt_erd]{dbdt_erd()}}, \code{\link[=dbdt_dm_mermaid]{dbdt_dm_mermaid()}},
\code{\link[=dbdt_dm_dot]{dbdt_dm_dot()}} and \code{\link[=dbdt_explorer]{dbdt_explorer()}} all draw.
}
\details{
Reverse-engineering a live connection uses \code{\link[=dbdt_tables]{dbdt_tables()}},
\code{\link[=dbdt_schema]{dbdt_schema()}} and \code{\link[=dbdt_relationships]{dbdt_relationships()}}, so it works against both
DuckDB and MS SQL Server. Only foreign keys the database actually declares
as constraints are picked up; DuckDB databases built from CSV/Parquet
loads usually declare none, so see \code{\link[=dbdt_dm_infer_references]{dbdt_dm_infer_references()}} (or
\code{infer_references = TRUE}) to guess them from column names, and
\code{\link[=dbdt_dm_add_references]{dbdt_dm_add_references()}} to state them by hand.
}
\examples{
con <- dbdt_connect(quiet = TRUE)
DBI::dbExecute(con, "CREATE TABLE customers (id INTEGER PRIMARY KEY, name VARCHAR)")
DBI::dbExecute(con, "CREATE TABLE orders (
  id INTEGER PRIMARY KEY, customer_id INTEGER REFERENCES customers(id), total DOUBLE)")

dm <- dbdt_data_model(con)
dm
dm$references
DBI::dbDisconnect(con, shutdown = TRUE)
}
\seealso{
\code{\link[=dbdt_erd]{dbdt_erd()}} to explore one in the browser,
\code{\link[=dbdt_explorer]{dbdt_explorer()}} for the Shiny version.
}
