% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/introspect.R, R/zz-dbdt.R
\name{duckdt_schema}
\alias{duckdt_schema}
\alias{dbdt_schema}
\title{List the columns of a database's tables}
\usage{
duckdt_schema(conn, table = NULL)

dbdt_schema(conn, table = NULL)
}
\arguments{
\item{conn}{A \code{DBI} connection, or a \code{"duckdt"} object (its \verb{$conn} is
used).}

\item{table}{Optionally, restrict to a single table name (matched against
\code{table_name} in any schema). \code{NULL} (the default) returns every table.}
}
\value{
A \code{data.table} with one row per column: \code{schema}, \code{table},
\code{column}, \code{type}, \code{ordinal_position}, \code{primary_key} (logical).
}
\description{
Queries the portable \code{information_schema.columns}/\code{table_constraints}
views (works against both DuckDB and MS SQL Server connections) for every
table's columns, their SQL data type, and whether each is part of a
primary key.
}
\examples{
con <- dbdt_example()
dbdt_schema(con, table = "orders")
dbdt_disconnect(con)
}
