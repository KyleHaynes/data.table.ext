% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/head-tail.R, R/zz-dbdt.R
\name{duckdt_sample}
\alias{duckdt_sample}
\alias{dbdt_sample}
\title{Sample rows from a duckdt table}
\usage{
duckdt_sample(x, n, method = c("random", "fast"))

dbdt_sample(x, n, method = c("random", "fast"))
}
\arguments{
\item{x}{A \code{"duckdt"} object.}

\item{n}{Number of rows to sample, a non-negative whole number.}

\item{method}{\code{"random"} (default) returns \code{n} random rows, or all rows
for a smaller table. On SQL Server this uses \verb{ORDER BY NEWID()} and
processes the entire table. \code{"fast"} uses SQL Server's
\verb{TABLESAMPLE SYSTEM} to read a sample of pages, capped at \code{n} rows.
It may return fewer rows (even zero), and rows on the same page are
sampled together; it is intended for exploration, not uniform random
sampling. It requires a local base table, not a view. On DuckDB both
methods use reservoir sampling.}
}
\value{
A \code{data.table} of at most \code{n} sampled rows.
}
\description{
Samples in the database, materializing the result as a \code{data.table}.
This is a plain function rather than a
\code{sample.duckdt} S3 method because base R's \code{\link[=sample]{sample()}} is not a true S3
generic (it doesn't call \code{UseMethod()}), so a \code{sample.duckdt} method would
never be dispatched by a plain \code{sample(d, ...)} call.
}
\examples{
d <- as.dbdt(datasets::mtcars)
dbdt_sample(d, 5)
}
