% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/head-tail.R
\name{duckdt_sample}
\alias{duckdt_sample}
\title{Sample rows from a duckdt table}
\usage{
duckdt_sample(x, n)
}
\arguments{
\item{x}{A \code{"duckdt"} object.}

\item{n}{Number of rows to sample.}
}
\value{
A \code{data.table} of \code{n} sampled rows.
}
\description{
Pushes DuckDB's \verb{USING SAMPLE} clause down to the database, materializing
\code{n} sampled rows as a \code{data.table}. This is a plain function rather than a
\code{sample.duckdt} S3 method because base R's \code{\link[=sample]{sample()}} is not a true S3
generic (it doesn't call \code{UseMethod()}), so a \code{sample.duckdt} method would
never be dispatched by a plain \code{sample(d, ...)} call.
}
