% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-model-verbs.R, R/zz-dbdt.R
\name{duckdt_dm_filter}
\alias{duckdt_dm_filter}
\alias{dbdt_dm_filter}
\title{Zoom a data model in on some tables}
\usage{
duckdt_dm_filter(dm, tables, depth = 0L, columns = NULL)

dbdt_dm_filter(dm, tables, depth = 0L, columns = NULL)
}
\arguments{
\item{dm}{A \code{"duckdt_data_model"} from \code{\link[=dbdt_data_model]{dbdt_data_model()}}.}

\item{tables}{Character vector of table names to keep.}

\item{depth}{Also keep tables within this many reference hops of
\code{tables}. Default \code{0} (just the named tables).}

\item{columns}{Optionally, a named list of character vectors -- names are
table names, values the columns to keep for that table. Tables not named
keep all their columns.}
}
\value{
A data model containing only the kept tables/columns, and only the
references between them.
}
\description{
Big schemas are unreadable all at once. This keeps only the tables you
name -- plus, with \code{depth}, whatever they connect to within that many
reference hops, which is usually what you actually wanted.
}
\examples{
dm <- dbdt_data_model(list(
  orders = data.frame(id = 1L, customer_id = 1L),
  customers = data.frame(id = 1L, name = "a"),
  unrelated = data.frame(z = 1)
))
dm <- dbdt_dm_add_references(dm, orders$customer_id == customers$id)
dbdt_dm_filter(dm, "orders", depth = 1)
}
