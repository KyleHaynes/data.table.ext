% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-model-verbs.R
\name{duckdt_dm_infer_references}
\alias{duckdt_dm_infer_references}
\title{Guess references from column naming conventions}
\usage{
duckdt_dm_infer_references(dm, quiet = FALSE)
}
\arguments{
\item{dm}{A \code{"duckdt_data_model"} from \code{\link[=duckdt_data_model]{duckdt_data_model()}}.}

\item{quiet}{Suppress the message summarising what was inferred.}
}
\value{
The data model, with inferred references added. The columns it
added are recorded in the \code{"inferred"} attribute of \verb{$references}.
}
\description{
Most DuckDB databases don't declare foreign keys, so
\code{\link[=duckdt_data_model]{duckdt_data_model()}} has nothing to draw between the boxes. This fills
that gap the way a human reading the schema would: a column is treated as
referencing table \code{t} when its name is \code{t}'s primary key (\code{customer_id}
pointing at \code{customers.customer_id}), or the table name glued to that key
(\code{customer_id} or \code{customerid} pointing at \code{customers.id}), with a naive
plural/singular fallback so \code{customers} and \code{customer} both match.
}
\details{
It is a guess, deliberately a conservative one. Columns that already have
a reference are left alone, a column that could point at more than one
table is skipped as ambiguous, a bare \code{id} is never treated as pointing
anywhere, and candidate pairs whose types are from different families
(integer vs. text, say) are rejected. Check the result -- print the model,
or look at \code{dm$references} -- rather than assuming it got everything
right, and state anything it missed with \code{\link[=duckdt_dm_add_references]{duckdt_dm_add_references()}}.

Tables need primary keys for this to have anything to aim at. If the
database declares none, set them first with \code{\link[=duckdt_dm_set_key]{duckdt_dm_set_key()}}.
}
\examples{
dm <- duckdt_data_model(list(
  customers = data.frame(customer_id = 1L, name = "a"),
  orders = data.frame(order_id = 1L, customer_id = 1L)
))
dm <- duckdt_dm_set_key(dm, "customers", "customer_id")
dm <- duckdt_dm_set_key(dm, "orders", "order_id")
dm <- duckdt_dm_infer_references(dm, quiet = TRUE)
dm$references
}
\seealso{
\code{\link[=duckdt_dm_add_references]{duckdt_dm_add_references()}} to state references exactly.
}
