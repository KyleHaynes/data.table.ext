% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm-mermaid.R
\name{duckdt_dm_mermaid}
\alias{duckdt_dm_mermaid}
\title{Render a data model as a Mermaid ER diagram}
\usage{
duckdt_dm_mermaid(dm, view = c("all", "keys_only", "title_only"), types = TRUE)
}
\arguments{
\item{dm}{A \code{"duckdt_data_model"} from \code{\link[=duckdt_data_model]{duckdt_data_model()}}, or anything
\code{\link[=duckdt_data_model]{duckdt_data_model()}} accepts (a connection, a \code{"duckdt"} handle, a
named list of data frames).}

\item{view}{How much of each table to draw: \code{"all"} columns,
\code{"keys_only"} (primary and foreign keys), or \code{"title_only"}.}

\item{types}{Show each column's SQL type. Default \code{TRUE}.}
}
\value{
A length-1 character vector of Mermaid \code{erDiagram} source.
}
\description{
Mermaid needs no R packages and renders anywhere Markdown does, so this is
what \code{\link[=duckdt_erd]{duckdt_erd()}} draws and what to paste into an Rmd/Quarto
\verb{```mermaid} chunk or a GitHub comment.
}
\examples{
dm <- duckdt_data_model(list(
  orders = data.frame(id = 1L, customer_id = 1L),
  customers = data.frame(id = 1L, name = "a")
))
dm <- duckdt_dm_add_references(dm, orders$customer_id == customers$id)
cat(duckdt_dm_mermaid(dm))
}
\seealso{
\code{\link[=duckdt_dm_dot]{duckdt_dm_dot()}} for the Graphviz rendering,
\code{\link[=duckdt_erd]{duckdt_erd()}} for the interactive browser page.
}
