% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm-dot.R, R/zz-dbdt.R
\name{duckdt_dm_dot}
\alias{duckdt_dm_dot}
\alias{dbdt_dm_dot}
\title{Render a data model as Graphviz DOT}
\usage{
duckdt_dm_dot(
  dm,
  view = c("all", "keys_only", "title_only"),
  col_attr = "column",
  rankdir = "BT",
  column_arrows = FALSE,
  graph_name = "Data model",
  graph_attrs = "",
  node_attrs = "",
  edge_attrs = ""
)

dbdt_dm_dot(
  dm,
  view = c("all", "keys_only", "title_only"),
  col_attr = "column",
  rankdir = "BT",
  column_arrows = FALSE,
  graph_name = "Data model",
  graph_attrs = "",
  node_attrs = "",
  edge_attrs = ""
)
}
\arguments{
\item{dm}{A \code{"duckdt_data_model"} from \code{\link[=dbdt_data_model]{dbdt_data_model()}}, or anything
\code{\link[=dbdt_data_model]{dbdt_data_model()}} accepts (a connection, a \code{"duckdt"} handle, a
named list of data frames).}

\item{view}{How much of each table to draw: \code{"all"} columns,
\code{"keys_only"} (primary and foreign keys), or \code{"title_only"} (just the
table names).}

\item{col_attr}{Which column attributes to show in each row. \code{"column"}
(the name) by default; \code{c("column", "type")} also shows SQL types.}

\item{rankdir}{Graphviz layout direction, e.g. \code{"BT"} (bottom to top,
the default), \code{"LR"}, \code{"TB"}, \code{"RL"}.}

\item{column_arrows}{Draw edges between the referencing and referenced
\emph{columns} rather than between the table boxes.}

\item{graph_name}{Graph tooltip/name.}

\item{graph_attrs, node_attrs, edge_attrs}{Extra Graphviz attribute strings,
appended to the defaults.}
}
\value{
A length-1 character vector of DOT source, with class
\code{"duckdt_dot"} so printing it shows the diagram (via \code{DiagrammeR}, if
installed) rather than the source text. Use \code{\link[=cat]{cat()}} to see the source.
}
\description{
A port of datamodelr's \code{dm_create_graph()}: each table becomes a
\code{plaintext} node whose label is an HTML table of its columns, primary keys
underlined, and each reference becomes an edge. Segments
(\code{\link[=dbdt_dm_set_segment]{dbdt_dm_set_segment()}}) become labelled clusters, and displays
(\code{\link[=dbdt_dm_set_display]{dbdt_dm_set_display()}}) pick the colours.
}
\examples{
dm <- dbdt_data_model(list(
  orders = data.frame(id = 1L, customer_id = 1L),
  customers = data.frame(id = 1L, name = "a")
))
dm <- dbdt_dm_add_references(dm, orders$customer_id == customers$id)
cat(substr(dbdt_dm_dot(dm), 1, 60))
}
\seealso{
\code{\link[=dbdt_dm_render]{dbdt_dm_render()}} to get a widget object directly,
\code{\link[=dbdt_dm_export]{dbdt_dm_export()}} to write an image file, \code{\link[=dbdt_erd]{dbdt_erd()}} for the
dependency-free browser version.
}
