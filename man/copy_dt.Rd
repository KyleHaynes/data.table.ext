% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-dt.R
\name{copy_dt}
\alias{copy_dt}
\title{Copy a data.table to the clipboard as a markdown table}
\usage{
copy_dt(dt, n = Inf, clip_fun = .write_clipboard)
}
\arguments{
\item{dt}{A data.table.}

\item{n}{Maximum number of rows to include. Default \code{Inf} (all rows).}

\item{clip_fun}{Function used to write the rendered text to the clipboard,
taking a single character scalar. Defaults to the package's built-in
cross-platform clipboard writer; override for testing or to route output
elsewhere.}
}
\value{
Invisibly returns the character scalar that was copied.
}
\description{
Renders \code{dt} as a markdown table (see \code{md_dt()}) and copies the result to
the system clipboard, for pasting into docs, PRs, or chat messages.
Supported on Windows and macOS out of the box; on Linux, requires \code{xclip}
or \code{xsel} to be installed.
}
