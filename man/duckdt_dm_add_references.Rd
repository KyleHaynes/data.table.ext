% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-model-verbs.R, R/zz-dbdt.R
\name{duckdt_dm_add_references}
\alias{duckdt_dm_add_references}
\alias{duckdt_dm_add_reference}
\alias{dbdt_dm_add_reference}
\alias{dbdt_dm_add_references}
\title{Add a reference (foreign key) to a data model}
\usage{
duckdt_dm_add_references(dm, ...)

duckdt_dm_add_reference(dm, table, column, ref, ref_col = NULL)

dbdt_dm_add_reference(dm, table, column, ref, ref_col = NULL)

dbdt_dm_add_references(dm, ...)
}
\arguments{
\item{dm}{A \code{"duckdt_data_model"} from \code{\link[=dbdt_data_model]{dbdt_data_model()}}.}

\item{...}{One or more expressions of the form
\code{table$column == ref_table$ref_column}. Nothing is evaluated -- the
names are read off the expression -- so the tables need not exist as R
objects.}

\item{table, column}{The referencing table and column(s).}

\item{ref, ref_col}{The referenced table and column(s). \code{ref_col} defaults
to \code{ref}'s primary key.}
}
\value{
The data model, with the reference(s) added.
}
\description{
DuckDB databases built by loading files rarely declare foreign keys, so
\code{\link[=dbdt_data_model]{dbdt_data_model()}} often comes back with nothing to draw between the
boxes. These two add them after the fact:
\code{dbdt_dm_add_references()} takes the readable
\code{table$column == other_table$other_column} form (a port of datamodelr's
\code{dm_add_references()}), and \code{dbdt_dm_add_reference()} takes plain
strings, for when the names are in variables.
}
\details{
Adding a reference also marks the referenced column as a key, since that
is what being referenced means.
}
\examples{
dm <- dbdt_data_model(list(
  orders = data.frame(id = 1L, customer_id = 1L),
  customers = data.frame(id = 1L, name = "a")
))
dm <- dbdt_dm_add_references(dm, orders$customer_id == customers$id)
dm$references
}
\seealso{
\code{\link[=dbdt_dm_infer_references]{dbdt_dm_infer_references()}} to guess them from column names.
}
