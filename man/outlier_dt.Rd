% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-eda.R
\name{outlier_dt}
\alias{outlier_dt}
\title{Flag rows containing outlier values}
\usage{
outlier_dt(dt, cols = NULL, method = c("iqr", "zscore"), threshold = NULL)
}
\arguments{
\item{dt}{A data.table.}

\item{cols}{Character vector of numeric column names to check. If \code{NULL}
(default), all numeric columns are checked.}

\item{method}{Character scalar: \code{"iqr"} (default) flags values outside
\code{threshold * IQR} from Q1/Q3. \code{"zscore"} flags values whose absolute
z-score exceeds \code{threshold}.}

\item{threshold}{Numeric scalar. For \code{"iqr"}, the IQR multiplier (default
\code{1.5}). For \code{"zscore"}, the z-score cutoff (default \code{3}).}
}
\value{
A data.table containing the flagged rows from \code{dt}, plus an
\code{outlier_cols} character column listing which column(s) triggered the
flag for that row. Returns a zero-row data.table if none are found.
}
\description{
Flags rows where any of \code{cols} (numeric columns) is an outlier under the
chosen \code{method}, returning only the flagged rows plus an \code{outlier_cols}
column listing which column(s) triggered the flag.
}
