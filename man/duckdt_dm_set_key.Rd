% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-model-verbs.R, R/zz-dbdt.R
\name{duckdt_dm_set_key}
\alias{duckdt_dm_set_key}
\alias{dbdt_dm_set_key}
\title{Mark columns as a table's primary key}
\usage{
duckdt_dm_set_key(dm, table, column)

dbdt_dm_set_key(dm, table, column)
}
\arguments{
\item{dm}{A \code{"duckdt_data_model"} from \code{\link[=dbdt_data_model]{dbdt_data_model()}}.}

\item{table}{Table name.}

\item{column}{Column name(s). For a compound key, pass them in key order.}
}
\value{
The data model, with the key set (and \verb{$references} rebuilt, since
a reference with no explicit \code{ref_col} points at the key).
}
\description{
Databases that declare their primary keys already have them in the model;
this is for the ones that don't (a DuckDB file loaded from CSV/Parquet,
say), and for models built from plain data frames.
}
\examples{
dm <- dbdt_data_model(list(people = data.frame(person_id = 1L, name = "a")))
dm <- dbdt_dm_set_key(dm, "people", "person_id")
dm$columns
}
