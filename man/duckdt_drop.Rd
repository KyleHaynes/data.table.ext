% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temp.R, R/zz-dbdt.R
\name{duckdt_drop}
\alias{duckdt_drop}
\alias{dbdt_drop}
\title{Drop a table created by dbdt_temp()}
\usage{
duckdt_drop(x, force = FALSE)

dbdt_drop(x, force = FALSE)
}
\arguments{
\item{x}{A \code{"duckdt"} object from \code{\link[=dbdt_temp]{dbdt_temp()}}.}

\item{force}{Drop the table even if \code{x} is not a \code{dbdt_temp()} handle?
Default \code{FALSE}. This deletes a real table -- there is no undo.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Temporary tables are yours to manage: DuckDB drops them when the
connection closes, but a long-lived session that builds many subsets will
hold them all until then (and on MS SQL Server they are ordinary tables
that persist). This drops one.
}
\details{
As a guard against dropping real data, this refuses handles that
\code{\link[=dbdt_temp]{dbdt_temp()}} did not create unless you pass \code{force = TRUE}.
}
