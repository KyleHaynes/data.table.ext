% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{duckdt_connect}
\alias{duckdt_connect}
\alias{duckdt_disconnect}
\title{Connect to a DuckDB database}
\usage{
duckdt_connect(dbdir = ":memory:", read_only = FALSE, quiet = FALSE, ...)

duckdt_disconnect(conn, shutdown = TRUE)
}
\arguments{
\item{dbdir}{Path to a DuckDB database file. Forward slashes work on
Windows and save escaping backslashes. The default, \code{":memory:"}, is a
scratch database that disappears when you disconnect. A file that
doesn't exist yet is created.}

\item{read_only}{Open the file read-only. A good default for a database
you only mean to explore, and required if another process has it open.}

\item{quiet}{Skip the connection summary.}

\item{...}{Passed to \code{\link[duckdb:duckdb]{duckdb::duckdb()}}.}

\item{conn}{A \code{DBI} connection, or a \code{"duckdt"} object (its connection is
closed).}

\item{shutdown}{Also shut the DuckDB database down, releasing the file.
Default \code{TRUE}.}
}
\value{
A \code{DBI} connection to DuckDB. Close it with
\code{\link[=duckdt_disconnect]{duckdt_disconnect()}} when you're done.

\code{duckdt_disconnect()} returns \code{TRUE} invisibly.
}
\description{
A thin, friendly wrapper around
\code{DBI::dbConnect(duckdb::duckdb(dbdir = ...))}. The only things it adds are
defaults worth having and a summary of what you just connected to: how
many tables are in there, and the two calls that get you moving --
\code{\link[=duckdt_erd]{duckdt_erd()}} to see the whole schema, \code{\link[=duckdt]{duckdt()}} to query one table with
\code{data.table} syntax.
}
\details{
You never have to use this: every function in duckdt takes a plain \code{DBI}
connection, so \code{DBI::dbConnect(duckdb::duckdb(dbdir = "x.duckdb"))} is
equally fine.
}
\examples{
con <- duckdt_connect()                       # in-memory scratch database
d <- as.duckdt(mtcars, conn = con, copy = TRUE)
d[cyl == 6, .(mpg, hp)]
duckdt_disconnect(con)

\dontrun{
# A database file on disk, read-only because we're only looking:
con <- duckdt_connect("C:/data/warehouse.duckdb", read_only = TRUE)
duckdt_erd(con)
}
}
\seealso{
\code{\link[=duckdt_erd]{duckdt_erd()}} to visualise the database you just opened,
\code{\link[=duckdt]{duckdt()}} to wrap one of its tables.
}
