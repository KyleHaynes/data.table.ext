% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explorer.R, R/zz-dbdt.R
\name{duckdt_explorer}
\alias{duckdt_explorer}
\alias{dbdt_explorer}
\title{Explore a database in a Shiny app}
\usage{
duckdt_explorer(
  x,
  tables = NULL,
  infer_references = FALSE,
  row_counts = FALSE,
  ...
)

dbdt_explorer(
  x,
  tables = NULL,
  infer_references = FALSE,
  row_counts = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{DBI} connection, a \code{"duckdt"} object, or a
\code{"duckdt_data_model"}. Given a model rather than a connection, the app
browses and builds queries but cannot run them.}

\item{tables}{Optionally, a character vector of tables to model.}

\item{infer_references}{Guess undeclared foreign keys from column naming
conventions, via \code{\link[=dbdt_dm_infer_references]{dbdt_dm_infer_references()}}.}

\item{row_counts}{Show each table's row count (a \verb{count(*)} per table).}

\item{...}{Passed to \code{\link[shiny:shinyApp]{shiny::shinyApp()}}.}
}
\value{
A Shiny app object. Printed at the console (or returned from a
\code{.R} file passed to \code{\link[shiny:runApp]{shiny::runApp()}}) it starts the app.
}
\description{
The interactive version of \code{\link[=dbdt_erd]{dbdt_erd()}}: the same tick-a-table,
tick-a-column browsing, but with a live diagram, the generated
\code{duckdt}/SQL code, and -- since it has a connection rather than a static
page -- a preview of the rows the query actually returns.
}
\details{
Needs the \code{shiny} package. The diagram is drawn with Graphviz via
\code{DiagrammeR} if that's installed, and falls back to showing the DOT
source if not; the row preview uses \code{DT} if that's installed. Nothing
beyond duckdt itself is needed for \code{\link[=dbdt_erd]{dbdt_erd()}}, which covers the same
ground in a plain HTML page.
}
\examples{
\dontrun{
con <- dbdt_connect("C:/data/warehouse.duckdb", read_only = TRUE)
dbdt_explorer(con, infer_references = TRUE)
}
}
\seealso{
\code{\link[=dbdt_erd]{dbdt_erd()}} for the dependency-free browser version.
}
